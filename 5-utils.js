// The value of sayHi variable is a function
const sayHi = (name) => {
    console.log(`Hello there ${name}`)
}

// export default, exporting function
module.exports = sayHi // it exports only one value unlike in 4-names.js where it export multiple objects 